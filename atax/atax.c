/****************************************************************/
/*   chebyshev		                                        */
/****************************************************************/

#ifdef TEST
#include <stdio.h>
#endif

#include<math.h>

#ifdef ALGO
void foo()
{
    int A [9];
    int x [3];
    int y [3];
    int i;
    {
        y[0] = (A[0] * (A[0] * x[0] + A[1] * x[1] + A[2] * x[2]) + A[3] * (A[3] * x[0] + A[4] * x[1] + A[5] * x[2]) + A[6] * (A[6] * x[0] + A[7] * x[1] + A[8] * x[2]));
        y[1] = (A[1] * (A[0] * x[0] + A[1] * x[1] + A[2] * x[2]) + A[4] * (A[3] * x[0] + A[4] * x[1] + A[5] * x[2]) + A[7] * (A[6] * x[0] + A[7] * x[1] + A[8] * x[2]));
        y[2] = (A[2] * (A[0] * x[0] + A[1] * x[1] + A[2] * x[2]) + A[5] * (A[3] * x[0] + A[4] * x[1] + A[5] * x[2]) + A[8] * (A[6] * x[0] + A[7] * x[1] + A[8] * x[2]));
    }

}
#endif


#ifdef TEST
int main(void)
{
	int x;
 	int result;
  	foo();
  	return 0;
}
#endif
